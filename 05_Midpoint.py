def midpoint(x1, y1, x2, y2):

    mid_x = (x1 + x2)/2
    mid_y = (y1 + y2)/2
    return mid_x, mid_y

midpoint_x_string = f"calculate mid_x: {midpoint}"
midpoint_y_string = f"calculate mid_y: {midpoint}"
